<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linrun.agent.infrastructure.dao.reactor.IToolOutputTodoWriteDao">

    <insert id="insert" parameterType="map">
        INSERT INTO tool_output_todo_write (
            tool_invocation_id, run_id, request_id, request_source, session_id, tool_call_id, status, error_msg,
            command, before_todo_json, after_todo_json, current_step, current_step_index, auto_advanced, auto_finished
        ) VALUES (
            #{row.toolInvocationId}, #{row.runId}, #{row.requestId}, #{row.requestSource}, #{row.sessionId}, #{row.toolCallId}, #{row.status}, #{row.errorMsg},
            #{row.command}, #{row.beforeTodoJson}, #{row.afterTodoJson}, #{row.currentStep}, #{row.currentStepIndex}, #{row.autoAdvanced}, #{row.autoFinished}
        )
    </insert>

    <select id="queryByToolInvocationId" parameterType="java.lang.Long" resultType="map">
        SELECT * FROM tool_output_todo_write
        WHERE tool_invocation_id = #{toolInvocationId}
        LIMIT 1
    </select>

    <select id="queryByRequestToolCall" resultType="map">
        SELECT * FROM tool_output_todo_write
        WHERE request_id = #{requestId} AND tool_call_id = #{toolCallId}
        LIMIT 1
    </select>

</mapper>
