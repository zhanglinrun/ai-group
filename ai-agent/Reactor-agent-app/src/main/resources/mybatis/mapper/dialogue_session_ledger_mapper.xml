<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.wwz.ai.infrastructure.dao.reactor.IDialogueSessionLedgerDao">

    <resultMap id="DialogueSessionMap" type="org.wwz.ai.domain.agent.ledger.entity.DialogueSession">
        <id column="id" property="id"/>
        <result column="session_id" property="sessionId"/>
        <result column="owner_id" property="ownerId"/>
        <result column="title" property="title"/>
        <result column="status" property="status"/>
        <result column="latest_request_id" property="latestRequestId"/>
        <result column="latest_query_text" property="latestQueryText"/>
        <result column="latest_summary_text" property="latestSummaryText"/>
        <result column="run_count" property="runCount"/>
        <result column="finished_run_count" property="finishedRunCount"/>
        <result column="failed_run_count" property="failedRunCount"/>
        <result column="started_at" property="startedAt"/>
        <result column="last_active_at" property="lastActiveAt"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <resultMap id="DialogueSessionViewMap" type="org.wwz.ai.domain.agent.ledger.model.DialogueSessionView">
        <id column="id" property="id"/>
        <result column="session_id" property="sessionId"/>
        <result column="owner_id" property="ownerId"/>
        <result column="title" property="title"/>
        <result column="status" property="status"/>
        <result column="latest_request_id" property="latestRequestId"/>
        <result column="latest_query_text" property="latestQueryText"/>
        <result column="latest_summary_text" property="latestSummaryText"/>
        <result column="run_count" property="runCount"/>
        <result column="finished_run_count" property="finishedRunCount"/>
        <result column="failed_run_count" property="failedRunCount"/>
        <result column="started_at" property="startedAt"/>
        <result column="last_active_at" property="lastActiveAt"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="entityColumns">
        id, session_id, owner_id, title, status, latest_request_id, latest_query_text, latest_summary_text,
        run_count, finished_run_count, failed_run_count, started_at, last_active_at, create_time, update_time, deleted
    </sql>

    <sql id="viewColumns">
        id, session_id, owner_id, title, status, latest_request_id, latest_query_text, latest_summary_text,
        run_count, finished_run_count, failed_run_count, started_at, last_active_at, create_time
    </sql>

    <insert id="upsertSession" parameterType="org.wwz.ai.domain.agent.ledger.model.DialogueSessionUpsertRecord">
        INSERT INTO dialogue_session (
            session_id, owner_id, title, status, latest_request_id, latest_query_text, latest_summary_text,
            run_count, finished_run_count, failed_run_count, started_at, last_active_at, create_time, update_time, deleted
        ) VALUES (
            #{sessionId}, #{ownerId}, #{title}, #{status}, #{latestRequestId}, #{latestQueryText}, #{latestSummaryText},
            #{runCount}, #{finishedRunCount}, #{failedRunCount}, #{startedAt}, #{lastActiveAt}, NOW(), NOW(), 0
        )
        ON DUPLICATE KEY UPDATE
            owner_id = VALUES(owner_id),
            title = VALUES(title),
            status = VALUES(status),
            latest_request_id = VALUES(latest_request_id),
            latest_query_text = VALUES(latest_query_text),
            latest_summary_text = VALUES(latest_summary_text),
            run_count = VALUES(run_count),
            finished_run_count = VALUES(finished_run_count),
            failed_run_count = VALUES(failed_run_count),
            last_active_at = VALUES(last_active_at),
            update_time = NOW()
    </insert>

    <select id="queryBySessionId" parameterType="java.lang.String" resultMap="DialogueSessionMap">
        SELECT <include refid="entityColumns"/>
        FROM dialogue_session
        WHERE session_id = #{sessionId} AND deleted = 0
        LIMIT 1
    </select>

    <select id="querySessionView" parameterType="java.lang.String" resultMap="DialogueSessionViewMap">
        SELECT <include refid="viewColumns"/>
        FROM dialogue_session
        WHERE session_id = #{sessionId} AND deleted = 0
        LIMIT 1
    </select>

    <select id="queryRecentSessions" parameterType="java.lang.Integer" resultMap="DialogueSessionViewMap">
        SELECT <include refid="viewColumns"/>
        FROM dialogue_session
        WHERE deleted = 0
        ORDER BY last_active_at DESC, id DESC
        LIMIT #{limit}
    </select>

    <select id="querySessionViewByOwner" resultMap="DialogueSessionViewMap">
        SELECT <include refid="viewColumns"/>
        FROM dialogue_session
        WHERE owner_id = #{ownerId} AND session_id = #{sessionId} AND deleted = 0
        LIMIT 1
    </select>

    <select id="queryRecentSessionsByOwner" resultMap="DialogueSessionViewMap">
        SELECT <include refid="viewColumns"/>
        FROM dialogue_session
        WHERE owner_id = #{ownerId} AND deleted = 0
        ORDER BY last_active_at DESC, id DESC
        LIMIT #{limit}
    </select>

</mapper>
