<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.wwz.ai.infrastructure.dao.reactor.IArtifactLedgerDao">

    <resultMap id="ArtifactRecordMap" type="org.wwz.ai.domain.agent.ledger.entity.ArtifactRecord">
        <id column="id" property="id"/>
        <result column="run_id" property="runId"/>
        <result column="request_id" property="requestId"/>
        <result column="tool_invocation_id" property="toolInvocationId"/>
        <result column="tool_call_id" property="toolCallId"/>
        <result column="artifact_role" property="artifactRole"/>
        <result column="visibility" property="visibility"/>
        <result column="source_type" property="sourceType"/>
        <result column="source_name" property="sourceName"/>
        <result column="file_name" property="fileName"/>
        <result column="storage_key" property="storageKey"/>
        <result column="download_url" property="downloadUrl"/>
        <result column="preview_url" property="previewUrl"/>
        <result column="mime_type" property="mimeType"/>
        <result column="file_size" property="fileSize"/>
        <result column="file_hash" property="fileHash"/>
        <result column="metadata_json" property="metadataJson"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="entityColumns">
        id, run_id, request_id, tool_invocation_id, tool_call_id, artifact_role, visibility, source_type, source_name,
        file_name, storage_key, download_url, preview_url, mime_type, file_size, file_hash, metadata_json,
        create_time, update_time, deleted
    </sql>

    <insert id="batchInsertArtifacts" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO artifact_record (
            run_id, request_id, tool_invocation_id, tool_call_id, artifact_role, visibility, source_type, source_name,
            file_name, storage_key, download_url, preview_url, mime_type, file_size, file_hash, metadata_json,
            create_time, update_time, deleted
        ) VALUES
        <foreach collection="records" item="item" separator=",">
            (
                #{item.runId}, #{item.requestId}, #{item.toolInvocationId}, #{item.toolCallId}, #{item.artifactRole},
                #{item.visibility}, #{item.sourceType}, #{item.sourceName}, #{item.fileName}, #{item.storageKey},
                #{item.downloadUrl}, #{item.previewUrl}, #{item.mimeType}, #{item.fileSize}, #{item.fileHash},
                #{item.metadataJson}, NOW(), NOW(), 0
            )
        </foreach>
    </insert>

    <select id="queryByRunId" parameterType="java.lang.Long" resultMap="ArtifactRecordMap">
        SELECT <include refid="entityColumns"/>
        FROM artifact_record
        WHERE run_id = #{runId} AND deleted = 0
        ORDER BY id ASC
    </select>

    <select id="queryByRunIds" resultMap="ArtifactRecordMap">
        SELECT <include refid="entityColumns"/>
        FROM artifact_record
        WHERE deleted = 0
        AND run_id IN
        <foreach collection="runIds" item="runId" open="(" separator="," close=")">
            #{runId}
        </foreach>
        ORDER BY run_id ASC, id ASC
    </select>

    <select id="queryByToolInvocationIds" resultMap="ArtifactRecordMap">
        SELECT <include refid="entityColumns"/>
        FROM artifact_record
        WHERE deleted = 0
        AND tool_invocation_id IN
        <foreach collection="toolInvocationIds" item="toolInvocationId" open="(" separator="," close=")">
            #{toolInvocationId}
        </foreach>
        ORDER BY tool_invocation_id ASC, id ASC
    </select>

    <select id="queryInputArtifactsByRunIds" resultMap="ArtifactRecordMap">
        SELECT <include refid="entityColumns"/>
        FROM artifact_record
        WHERE deleted = 0
        AND artifact_role = 'input'
        AND run_id IN
        <foreach collection="runIds" item="runId" open="(" separator="," close=")">
            #{runId}
        </foreach>
        ORDER BY run_id ASC, id ASC
    </select>

    <select id="queryOutputArtifactsByToolInvocationId" parameterType="java.lang.Long" resultMap="ArtifactRecordMap">
        SELECT <include refid="entityColumns"/>
        FROM artifact_record
        WHERE tool_invocation_id = #{toolInvocationId}
        AND artifact_role = 'output'
        AND deleted = 0
        ORDER BY id ASC
    </select>

    <select id="queryOutputArtifactsByRunIdAndToolCallId" resultMap="ArtifactRecordMap">
        SELECT <include refid="entityColumns"/>
        FROM artifact_record
        WHERE run_id = #{runId}
        AND tool_call_id = #{toolCallId}
        AND artifact_role = 'output'
        AND deleted = 0
        ORDER BY id ASC
    </select>

    <select id="queryOutputArtifactsByRequestIdAndToolCallId" resultMap="ArtifactRecordMap">
        SELECT <include refid="entityColumns"/>
        FROM artifact_record
        WHERE request_id = #{requestId}
        AND tool_call_id = #{toolCallId}
        AND artifact_role = 'output'
        AND deleted = 0
        ORDER BY id ASC
    </select>

</mapper>
