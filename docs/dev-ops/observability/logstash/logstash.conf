input {
  tcp {
    port => 15044
    codec => json_lines
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "ai-group-logstash-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
