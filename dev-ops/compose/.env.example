# Canonical environment template: ../../.env.example
# From the repository root run:
#   Copy-Item .env.example .env
#   docker compose --env-file .env -f dev-ops/compose/docker-compose.full.yml up --build
#
# This file intentionally contains no second set of defaults. Keeping one
# template prevents service and Compose credentials from drifting apart.
